services:
  # Backend Service (FastAPI + Collectors)
  - type: web
    name: situational-awareness-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: PORT
        value: 8080
    autoDeploy: false
